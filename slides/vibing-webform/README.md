# Vibing Webform Slides

Run the deck locally with:

```bash
npm install
ddev slides vibing-webform
```

For the live Drupal demo links used in the presentation, prepare the site with:

```bash
ddev install webform-setup
```
