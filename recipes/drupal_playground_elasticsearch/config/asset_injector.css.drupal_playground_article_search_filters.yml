langcode: en
status: true
dependencies:
  module:
    - asset_injector
id: drupal_playground_article_search_filters
label: 'Drupal Playground Article Search Filters'
code: |-
  body.path-search .form-checkboxes.bef-checkboxes.bef-nested ul,
  body.path-search .form-checkboxes.bef-checkboxes.bef-nested li {
    list-style: none;
    margin-left: 0;
    padding-left: 0;
  }

  body.path-search .view-filters form.views-exposed-form.bef-exposed-form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  body.path-search .view-filters .form-item-keys {
    order: 1;
    width: 100%;
    margin: 0;
  }

  body.path-search .view-filters .form-item-keys input {
    width: 100%;
    max-width: 100%;
    box-sizing: border-box;
  }

  body.path-search .view-filters .form-actions {
    order: 2;
    margin: 0;
  }

  body.path-search .view-filters fieldset {
    order: 3;
    margin: 0;
  }

  body.path-search .view-content .teaser__meta:empty {
    display: none;
  }

  @media (min-width: 900px) {
    body.path-search .view {
      display: grid;
      grid-template-columns: minmax(280px, 35%) minmax(0, 65%);
      grid-template-areas:
        "filters summary"
        "filters results";
      column-gap: 2rem;
      row-gap: 0.5rem;
      align-items: start;
    }

    body.path-search .view-filters {
      grid-area: filters;
      margin: 0;
    }

    body.path-search form.views-exposed-form.bef-exposed-form {
      margin: 0;
    }

    body.path-search .view-header {
      grid-area: summary;
      margin: 0 0 1rem 0;
    }

    body.path-search .view-content,
    body.path-search .view-empty {
      grid-area: results;
      min-width: 0;
    }
  }
conditions_require_all: true
conditions:
  request_path:
    id: request_path
    negate: false
    pages: /search/articles
contexts: {  }
media: all
preprocess: true
