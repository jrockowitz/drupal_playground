The files in the directory are snapshots of exported configuration that 
will be updated via the recipe.yml.

Using the steps and scripts in DEVELOPMENT.md you can update these files 
and perform a diff to determine which configuration changes need to be 
applied to a recipe.
