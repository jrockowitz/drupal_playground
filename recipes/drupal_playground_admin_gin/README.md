# Drupal Playground Admin Gin

Sets up Gin as the Drupal Playground admin theme.

This recipe contains the Gin-specific modules, block configuration, and admin
theme setting that previously lived in `drupal_playground_admin`.

Use this recipe for Drupal 10 installs. The `ddev install` command applies it
automatically when the installed Drupal major version is less than 11.
