# Schema.org Blueprints issue maintenance

Public research notes for the Schema.org Blueprints issue queue. They are a
read-only triage snapshot, not a record of private reports or a substitute for
maintainer decisions.

Update an issue note first, then the dashboard in `index.md`. Each note links
to the canonical public issue instead of duplicating the full conversation.
