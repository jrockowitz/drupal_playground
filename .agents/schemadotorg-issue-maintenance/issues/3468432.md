# #3468432: If I want to add `additionalProperty` under the Thing type, what should I do?

## Context

- URL: https://www.drupal.org/node/3468432
- Status: Active; documentation support request in `1.0.x-dev`.
- Lane: Resolved support follow-up.
- Target branch: `1.0.x`.

## Evidence

- The reporter attempted to edit bundled Schema.org CSV data to add `additionalProperty` to Thing and asks for hooks or services instead.
- The initial response recommends `schemadotorg_additional_mappings` to apply a Schema.org type to node mappings, then configure its default properties.
- The unresolved follow-up asks for the Custom Field `map` data type instead of the generated plain-text field.
- Inspected the issue and all public comments on 2026-07-28 with `drupalorg issue:show 3468432 --with-comments --format=llm --no-cache`.
- Related issue [#3452343](https://www.drupal.org/node/3452343) is Fixed. Its merged [MR !313](https://git.drupalcode.org/project/schemadotorg/-/merge_requests/313) adds Custom Field map-to-JSON-LD support: complete map rows for `additionalProperty` are emitted as `PropertyValue` objects, with Kernel coverage and README guidance.
- Verified the MR scope with `drupalorg mr:files 'project/schemadotorg!313' --format=llm --no-cache` and `drupalorg mr:diff 'project/schemadotorg!313' --format=llm --no-cache`.

## Suggested comment

```markdown
From Codex

I reviewed this against `1.0.x`.

What I checked:
- The supported approach remains configuring the extra Schema.org type through `schemadotorg_additional_mappings`; the bundled CSV files should not be edited.
- The Custom Field map follow-up is now covered by [#3452343](https://www.drupal.org/project/schemadotorg/issues/3452343), which has been merged in [MR !313](https://git.drupalcode.org/project/schemadotorg/-/merge_requests/313).
- A `map` subfield configured for `additionalProperty` stores repeatable Name/Value rows and emits each complete row as a `PropertyValue` JSON-LD object.

Commands run:
- `drupalorg issue:show 3468432 --with-comments --format=llm --no-cache`
- `drupalorg mr:diff 'project/schemadotorg!313' --format=llm --no-cache`

Result:
- Use the Custom Field settings for the relevant mapped Schema.org type and set the `additionalProperty` subfield data type to `map`; do not alter the bundled Schema.org CSV files. The `schemadotorg_custom_field` README now documents this support.

Remaining question:
- After deploying the merge, is the affected mapping newly generated or re-applied so its Custom Field configuration includes the map subfield?
```

## Next action

Review the proposed reply with the maintainer, then post it and mark the issue Fixed only with explicit approval.
