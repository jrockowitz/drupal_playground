# #3623687: Prevent fatal error on programmatic node saves without an HTTP request in Schema.org Devel

## Context

- URL: https://www.drupal.org/node/3623687
- Status: Active; bug report in `1.0.x-dev`.
- Lane: Scoped fix and regression test.
- Canonical issue project: `schemadotorg`.
- Code project and MR target: `schemadotorg_experimental`, branch `1.0.x`.
- Local code branch: `codex/3623687-null-request`; MR: pending.

## Evidence

- Inspected the issue and comments with `drupalorg issue:show 3623687 --with-comments --format=llm --no-cache` on 2026-09-16. No prior discussion or MR exists.
- The main-project issue fork exists, but the affected `schemadotorg_devel` code is in the experimental project. The maintainer chose a branch in `schemadotorg_experimental` for the code MR.
- `SchemaDotOrgDevelGenerate::nodePresave()` calls `isDevelGenerateRequest()` without checking for an HTTP request. The nullable `request()` helper can return NULL, causing the reported fatal error.
- Local changes guard `isDevelGenerateRequest()` and `isGet()`, correct the request helper's return documentation, and add a focused unit test.
- `ddev phpunit` passes for the new unit test (1 test, 4 assertions) and the existing Devel functional test (1 test, 16 assertions; 24 deprecations).
- `ddev code-review` reported no PHP CodeSniffer errors. Its PHPStan errors in the service concern unchanged field-item property accesses; the new test passed PHPStan. The command also applied JavaScript and CSS linters to PHP files, producing parser errors.

## Next action

Obtain maintainer review of the full diff before committing or pushing, then push the experimental branch and create an MR targeting `1.0.x`.
