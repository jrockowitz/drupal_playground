# #3611136: Prevent fatal error on programmatic node saves without an HTTP request in Schema.org Devel

## Context

- URL: https://git.drupalcode.org/project/schemadotorg_experimental/-/work_items/3611136
- Original Drupal.org issue: https://www.drupal.org/node/3623687 (still open).
- Status: Open; bug report for `1.0.x-dev`.
- Lane: Scoped fix and regression test.
- Issue and code project: `schemadotorg_experimental`; MR target: `1.0.x`.
- Issue fork: `issue/schemadotorg_experimental-3611136`.
- Branch: `3611136-null-request`; draft MR: https://git.drupalcode.org/project/schemadotorg_experimental/-/merge_requests/16.

## Evidence

- The experimental work item was created from the Drupal.org report on 2026-09-17. Its description links to the original issue and retains the bug, code, normal priority, and `v1.0.x-dev` labels.
- The maintainer authorized creation of the experimental issue fork, commit, push, and MR. The fork branch was created from `1.0.x`.
- `SchemaDotOrgDevelGenerate::nodePresave()` calls `isDevelGenerateRequest()` without checking for an HTTP request. The nullable `request()` helper can return NULL, causing the reported fatal error.
- Local changes guard `isDevelGenerateRequest()` and `isGet()`, correct the request helper's return documentation, and add a focused unit test.
- `ddev phpunit` passes for the new unit test (1 test, 4 assertions) and the existing Devel functional test (1 test, 16 assertions; 24 deprecations).
- `ddev code-review` reported no PHP CodeSniffer errors. Its PHPStan errors in the service concern unchanged field-item property accesses; the new test passed PHPStan. The command also applied JavaScript and CSS linters to PHP files, producing parser errors.
- Commit `4df40f7` includes the required `AI-assisted by Codex` note and is pushed to the issue fork. Draft MR !16 contains exactly the service and new unit test.
- MR pipeline 965663 failed its PHPUnit job twice, while the new unit test passed. The existing Devel functional test failed because `title[0][value]` was absent from the form. The latest `1.0.x` pipeline 962376 failed on the same test and assertion before this patch; PHPStan also fails on the target branch. Keep the MR in draft until the baseline CI issue is resolved or reviewed.

## Next action

Have the maintainer review draft MR !16 and its target-branch CI baseline before marking it ready. The old Drupal.org issue remains open and may need a cross-link or closure decision.
